<Window x:Class="SimulacionCmi.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Simulación CMI" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Visitas:" VerticalAlignment="Center"/>
            <TextBox Width="80" Margin="5,0" Text="{Binding Visitas}"/>
            <TextBlock Text="Ventas objetivo:" VerticalAlignment="Center" Margin="20,0,0,0"/>
            <TextBox Width="80" Margin="5,0" Text="{Binding VentasObjetivo}"/>
            <Button Content="Simular" Click="Simular_Click" Margin="20,0,0,0"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="P(Def. sí):"/>
            <TextBlock Text="{Binding ProbabilidadDefSi, StringFormat=P2}" Margin="5,0,20,0"/>
            <TextBlock Text="Visita objetivo:"/>
            <TextBlock Text="{Binding VisitaObjetivo}" Margin="5,0,20,0"/>
            <TextBlock Text="Visitas analíticas:"/>
            <TextBlock Text="{Binding VisitasAnaliticas, StringFormat=F0}" Margin="5,0,0,0"/>
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Vectores}" AutoGenerateColumns="True"/>
    </Grid>
</Window>
