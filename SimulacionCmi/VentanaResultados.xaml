<Window x:Class="SimulacionCmi.VentanaResultados"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resultados" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid Grid.Row="0" ItemsSource="{Binding Vectores}" AutoGenerateColumns="True"/>
        <Button Grid.Row="1" Content="Ver gráficos" Click="VerGraficos_Click" HorizontalAlignment="Right" Margin="0,10,0,0"/>
    </Grid>
</Window>
